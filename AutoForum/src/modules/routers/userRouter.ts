import http from "http";

const userRouter = (request: http.IncomingMessage, response: http.ServerResponse, pathName : string) => {
    switch(pathName){
        case "create":{
            break;
        }
    }
}

export = userRouter